# 4ViviTools Run Log - 2026-07-13

## Main Codex

- Created `CONTRACTS.md` from the overnight master plan.
- Continuing with FocusGate, trusted percent health, OCR hard-attach, and Release build verification.
- Spawned agents:
  - Dewey: safety/input audit.
  - Plato: OCR health audit.
  - Epicurus: Smart Bot combat/state audit.
  - Newton: UI cleanup proposal.
- Vision Assist runtime now prefers the built-in mob-id marker table; the user only needs the GRF loaded in DATA.INI.
- Converted Smart Bot stuck/focus-kill timing to milliseconds with legacy seconds migration.
- Smart Bot now requires two consecutive low-HP/vanished-target scans before confirming a kill, and skill cooldown starts after the actual key/click sequence.
- Removed automatic SetForegroundWindow calls from input send paths; FocusGate now expects the RO client to already be foreground before actions leave.
- Fixed the new SliderWithBox leaf control to use Avalonia-compatible spacing; Release build returned green afterward.
- Added minimal SmartBotState transition logging at existing combat-loop decision boundaries.

## Health/OCR Runtime Cleanup - 2026-07-13 21:58:28
- Discord RPC now emits trusted HP/SP percentages only; flat current/max HP/SP removed from presence payload.
- Stats death tracking now observes trusted HP percent instead of flat HP.
- HP/SP percent OCR marks are pinned to Paddle and fallback OCR results are held instead of published as trusted vitals.
- Legacy vital HP/SP color-bar fallback block removed from OcrService.
- Verification: dotnet build Release --no-restore passed; dotnet test Core.Tests Release --no-build passed 74/74.

